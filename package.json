{
  "name": "backup-agent",
  "version": "1.5.0",
  "description": "Self-contained Windows backup agent for scheduled SSH/SFTP file transfers.",
  "main": "src/index.js",
  "bin": {
    "backup-agent": "src/cli.js"
  },
  "scripts": {
    "start": "node src/index.js",
    "cli": "node src/cli.js",
    "test": "node --test",
    "check": "node --check src/index.js && node --check src/cli.js && node --check src/config.js && node --check src/logger.js && node --check src/metrics.js && node --check src/notifications.js && node --check src/transfer.js",
    "package:win": "node scripts/package-windows.js"
  },
  "pkg": {
    "scripts": [
      "src/**/*.js"
    ],
    "assets": []
  },
  "dependencies": {
    "axios": "^1.7.9",
    "dotenv": "^16.4.7",
    "node-cron": "^4.5.0",
    "nodemailer": "^9.0.3",
    "socks": "^2.8.3",
    "socks-proxy-agent": "^8.0.4",
    "ssh2": "^1.16.0"
  },
  "engines": {
    "node": ">=24"
  },
  "license": "GPL-3.0-only"
}
